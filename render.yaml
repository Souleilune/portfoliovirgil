services:
  - type: web
    name: portfolio
    env: node
    buildCommand: npm install && npm run build
    startCommand: npm start
    envVars:
      - key: NODE_ENV
        value: production
    # Exclude components folder from deployment if needed
    # The components are already in the codebase, so they'll be included
    # but ESLint won't check them during build





